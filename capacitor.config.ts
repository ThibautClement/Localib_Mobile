import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exoIonic_profil',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
